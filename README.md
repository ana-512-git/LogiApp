## Setup and running the code:
`docker-compose up --build -d`

## Running the seeder:
Navigate to /backend/ and run:
`docker exec -it inventory_backend npm run seed`

## Shutting down:
`docker-compose down`
